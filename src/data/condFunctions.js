// サービスIDをキーにしたマッチング条件関数のマップ
// Google Sheetsからデータを取得した際に、IDに対応するcond関数を付与するために使用

export const COND_FUNCTIONS = {
  1:  (s) => s.children.length > 0,
  2:  (s) => s.children.length > 0,
  3:  (s) => s.children.some(c => (c.age||c) <= 5),
  4:  (s) => s.children.some(c => (c.age||c) <= 5),
  5:  (s) => s.children.some(c => (c.age||c) >= 6 && (c.age||c) <= 12),
  6:  (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  7:  (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  8:  (s) => s.sit?.includes('pregnant') || s.concerns?.includes('pregnant'),
  9:  (s) => s.sit?.includes('pregnant') || s.concerns?.includes('pregnant'),
  10: (s) => s.sit?.includes('pregnant') || s.concerns?.includes('pregnant') || s.children.some(c => (c.age||c) === 0),
  11: (s) => parseInt(s.age) >= 65 || s.sit?.includes('nursing') || s.elderlyMembers?.length > 0,
  12: (s) => parseInt(s.age) >= 65 || s.elderlyMembers?.length > 0,
  13: (s) => parseInt(s.age) >= 65 || s.elderlyMembers?.some(e => parseInt(e.age) >= 65),
  14: (s) => parseInt(s.age) >= 40 && parseInt(s.age) <= 74,
  15: (s) => parseInt(s.age) >= 20,
  16: (s) => parseInt(s.age) >= 65 || s.elderlyMembers?.length > 0,
  17: (s) => s.income === 'low' || s.income === 'nontax' || s.sit?.includes('lowincome'),
  18: (s) => s.income === 'low' || s.income === 'nontax' || s.sit?.includes('lowincome'),
  19: (s) => s.income === 'low' || s.income === 'nontax' || s.concerns?.includes('money'),
  20: (s) => s.income === 'low' || s.income === 'nontax' || s.concerns?.includes('housing_concern'),
  21: (s) => s.employment === 'unemployed' || s.concerns?.includes('work'),
  22: (s) => s.employment === 'unemployed' || s.concerns?.includes('work'),
  23: (s) => s.disabledMembers?.includes('disabled') || s.disabledMembers?.includes('intellectual') || s.disabledMembers?.includes('mental'),
  24: (s) => s.disabledMembers?.includes('disabled') || s.disabledMembers?.includes('intellectual') || s.disabledMembers?.includes('mental') || s.disabledMembers?.includes('gray'),
  25: (s) => s.disabledMembers?.includes('disabled') || s.disabledMembers?.includes('gray') || s.concerns?.includes('disability_service'),
  26: (s) => s.disabledMembers?.includes('disabled') || s.disabledMembers?.includes('gray') || s.concerns?.includes('disability_service'),
  27: (s) => s.disabledMembers?.includes('disabled') || s.disabledMembers?.includes('gray') || s.concerns?.includes('disability_service'),
  28: (s) => s.disabledMembers?.includes('disabled') || s.disabledMembers?.includes('gray') || s.concerns?.includes('mental_health'),
  29: (s) => s.sit?.includes('pregnant') || s.concerns?.includes('pregnant') || s.children.some(c => (c.age||c) === 0),
  30: (s) => s.sit?.includes('pregnant') || s.concerns?.includes('pregnant') || s.children.some(c => (c.age||c) <= 1),
  31: (s) => s.children.length > 0,
  32: (s) => s.children.some(c => (c.age||c) <= 3) || s.concerns?.includes('pregnant'),
  33: (s) => s.children.length >= 2,
  34: (s) => s.children.length > 0 || s.concerns?.includes('pregnant'),
  35: (s) => s.disabledMembers?.includes('hikikomori') || s.concerns?.includes('hikikomori_concern') || s.children?.some(c => c.status === 'futoko'),
  36: (s) => s.disabledMembers?.includes('hikikomori') || s.concerns?.includes('hikikomori_concern'),
  37: (s) => s.disabledMembers?.includes('hikikomori') || s.disabledMembers?.includes('gray') || s.disabledMembers?.includes('mental') || s.concerns?.includes('mental_health'),
  38: (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  39: (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  40: (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  41: (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  42: (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  43: (s) => s.elderlyMembers?.length > 0 && (s.living === 'alone' || s.elderlyMembers?.length >= 1),
  44: (s) => s.elderlyMembers?.length > 0,
  45: (s) => s.elderlyMembers?.some(e => ['c1','c2','c3','c4','c5','s1','s2'].includes(e.careLevel)),
  46: (s) => s.elderlyMembers?.length > 0,
  47: (s) => s.elderlyMembers?.some(e => ['c3','c4','c5'].includes(e.careLevel)),
  48: (s) => s.elderlyMembers?.length > 0 && (s.income === 'low' || s.income === 'nontax'),
  49: (s) => s.concerns?.includes('nursing') || s.elderlyMembers?.some(e => ['c4','c5'].includes(e.careLevel)),
  50: (s) => s.elderlyMembers?.length > 0,
  51: (s) => s.elderlyMembers?.length > 0 || s.disabledMembers?.includes('disabled'),
  52: (s) => s.elderlyMembers?.length > 0 && s.living === 'alone',
  53: (s) => s.elderlyMembers?.some(e => parseInt(e.age) >= 60) || parseInt(s.age) >= 60,
  54: (s) => s.elderlyMembers?.length > 0 || s.disabledMembers?.includes('disabled'),
  55: (_) => true,
  56: (s) => s.children.length > 0,
  57: (_) => true,
  58: (s) => s.children.length > 0,
  59: (_) => true,
  60: (s) => s.disabledMembers?.includes('mental') || s.disabledMembers?.includes('gray') || s.concerns?.includes('mental_health'),
  61: (s) => s.children.some(c => (c.age||c) <= 5),
  62: (s) => s.children.some(c => (c.age||c) <= 12),
  63: (s) => s.children.some(c => (c.age||c) <= 5),
  64: (s) => s.children.some(c => (c.age||c) <= 5),
  65: (s) => s.children.some(c => (c.age||c) >= 3 && (c.age||c) <= 5),
  66: (s) => s.children.some(c => (c.age||c) >= 6 && (c.age||c) <= 12),
  67: (s) => s.children.some(c => (c.age||c) >= 6 && (c.age||c) <= 12),
  68: (s) => s.children.some(c => (c.age||c) <= 5) || s.concerns?.includes('pregnant'),
  69: (s) => s.disabledMembers?.includes('hikikomori') || s.concerns?.includes('hikikomori_concern') || s.concerns?.includes('education') || s.concerns?.includes('child_disability') || s.children?.some(c => c.status === 'futoko') || parseInt(s.age) <= 39,
  70: (s) => s.children.some(c => (c.age||c) === 0) || s.concerns?.includes('pregnant'),
  71: (s) => s.children.length > 0 || s.concerns?.includes('childcare'),
  72: (s) => s.children.some(c => (c.age||c) >= 6 && (c.age||c) <= 15) && (s.income === 'low' || s.income === 'nontax'),
  73: (s) => s.children.some(c => (c.age||c) >= 14 && (c.age||c) <= 16) && (s.income === 'low' || s.income === 'nontax'),
  74: (s) => s.children?.some(c => c.status === 'futoko') || s.concerns?.includes('education') || s.concerns?.includes('hikikomori_concern'),
  75: (s) => s.children.length > 0 || s.concerns?.includes('dv') || s.concerns?.includes('child_disability'),
  76: (s) => s.children.length > 0 || s.concerns?.includes('dv') || s.concerns?.includes('education'),
  77: (s) => s.disabledMembers?.includes('intellectual') || s.disabledMembers?.includes('gray') || s.concerns?.includes('child_disability') || s.children?.some(c => c.status === 'special'),
  78: (s) => s.concerns?.includes('dv'),
  79: (s) => s.concerns?.includes('consumer') || parseInt(s.age) >= 60,
  80: (s) => s.concerns?.includes('foreign'),
  81: (s) => s.elderlyMembers?.length > 0 || s.disabledMembers?.includes('intellectual') || s.disabledMembers?.includes('mental') || s.concerns?.includes('nursing'),
  82: (s) => s.income === 'low' || s.income === 'nontax' || s.concerns?.includes('money'),
  83: (s) => s.concerns?.includes('pregnant') || s.sit?.includes('pregnant') || s.concerns?.includes('dv'),
  84: (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  85: (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  86: (s) => s.marital === 'div' && s.children.length > 0,
  87: (s) => (s.marital === 'div' || s.marital === 'widow') && s.children.length > 0,
  88: (s) => parseInt(s.age) >= 75 || s.elderlyMembers?.some(e => parseInt(e.age) >= 75),
  89: (s) => s.concerns?.includes('disaster'),
  90: (s) => s.concerns?.includes('disaster'),
  91: (s) => s.concerns?.includes('disaster') && (s.elderlyMembers?.length > 0 || s.disabledMembers?.length > 0 || parseInt(s.age) >= 65),
  92: (s) => s.concerns?.includes('disaster'),
  93: (s) => s.concerns?.includes('infertility'),
  94: (s) => s.concerns?.includes('dementia') || s.elderlyMembers?.length > 0,
  95: (s) => s.concerns?.includes('vaccination') || s.children?.some(c => (c.age||c) <= 12),
  96: (s) => s.concerns?.includes('admin'),
  97: (s) => s.concerns?.includes('tax') || s.employment === 'unemployed' || s.sit?.includes('lowincome'),
  98: (s) => s.concerns?.includes('waste'),
  99: (s) => s.concerns?.includes('transport') && (s.disabledMembers?.length > 0 || parseInt(s.age) >= 65 || s.elderlyMembers?.length > 0),
  100: (s) => s.concerns?.includes('pet'),
};
